{
  "name": "email-studio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "pnpm with-env email dev --dir=../../packages/email/src/templates --port=7001",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "devDependencies": {
    "@nzc/tsconfig": "workspace:*",
    "dotenv-cli": "catalog:",
    "react-email": "^4.0.7",
    "typescript": "catalog:"
  }
}
